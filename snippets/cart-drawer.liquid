{%- doc -%}
  Renders the cart drawer, a slide-out panel that displays the contents of the cart. It includes the cart icon that acts as a trigger.

  @param {object} [settings] - An object containing theme settings.
  @param {boolean} [settings.auto_open_cart_drawer] - If `true`, the cart drawer opens automatically after an item is added.
  @param {string} [settings.drawer_color_scheme] - The color scheme for the drawer.
{%- enddoc -%}

<script
  src="{{ 'cart-drawer.js' | asset_url }}"
  type="module"
  fetchpriority="low"
></script>

<cart-drawer-component
  class="cart-drawer"
  {{ block.shopify_attributes }}
  {% if settings.auto_open_cart_drawer %}
    auto-open
  {% endif %}
>
  <button
    class="button header-actions__action button-unstyled"
    on:click="/open"
    aria-label="{{ 'accessibility.open_cart_drawer' | t }} {{ 'accessibility.cart_count' | t}}: {{ cart.item_count }}"
  >
    {% render 'cart-icon-component' %}
  </button>

  <dialog
    ref="dialog"
    class="cart-drawer__dialog dialog-modal dialog-drawer color-{{ settings.drawer_color_scheme }}{% if cart.empty? %} cart-drawer--empty{%endif%}"
    scroll-lock
  >
    <div class="cart-drawer__inner">
      <cart-items-component
        class="cart-items-component"
        data-section-id="{{ section.id }}"
      >
        {%- if cart.empty? -%}
          <div class="cart-drawer__header">
            <span class="cart-drawer__heading h3">
              0 Artículos en el carrito
            </span>
            <button
              ref="closeButton"
              on:click="cart-drawer-component/close"
              class="button close-button cart-drawer__close-button button-unstyled"
              aria-label="{{ 'actions.close_dialog' | t }}"
            >
              <span class="svg-wrapper">
                {{- 'icon-close.svg' | inline_asset_content -}}
              </span>
            </button>
          </div>

          {%- comment -%} Barra de progreso de envío gratis {%- endcomment -%}
          {% assign free_shipping_threshold = 10000 %}
          {% assign cart_total = cart.total_price %}
          {% assign remaining = free_shipping_threshold | minus: cart_total %}
          {% assign progress_percentage = cart_total | times: 100.0 | divided_by: free_shipping_threshold %}
          {% if progress_percentage > 100 %}
            {% assign progress_percentage = 100 %}
          {% endif %}
          
          <div class="cart-drawer__free-shipping-bar">
            <div class="free-shipping-bar__text">
              {% if remaining > 0 %}
                Debes agregar al menos una cafetera de la familia Aurora para tener envío gratis
              {% else %}
                <strong>¡Felicidades! Calificas para envío gratis!</strong>
              {% endif %}
            </div>
            <div class="free-shipping-bar__progress">
              <div class="free-shipping-bar__progress-fill" style="width: {{ progress_percentage }}%"></div>
            </div>
          </div>

          <div
            class="cart-drawer__content motion-reduce"
            aria-label="{{ 'accessibility.cart' | t }}"
          >
            <div class="cart-drawer__empty-state">
              <span class="cart-drawer__empty-text">
                Tu carrito está vacío
              </span>
              <a href="/collections/all" class="cart-drawer__cta-button">
                Ver todos los productos
              </a>
              
              {%- comment -%} Trust Badges para carrito vacío {%- endcomment -%}
              <div class="cart-drawer__trust-badges">
                <div class="trust-badges__grid">
                  <div class="trust-badge">
                    <div class="trust-badge__icon">
                      <svg viewBox="0 0 256 256" fill="currentColor">
                        <path d="M208,40H48A16,16,0,0,0,32,56v58.77c0,89.61,75.82,119.34,91,124.39a15.53,15.53,0,0,0,10,0c15.2-5.05,91-34.78,91-124.39V56A16,16,0,0,0,208,40Zm0,74.79c0,78.42-66.35,104.62-80,109.18-13.53-4.51-80-30.69-80-109.18V56H208ZM58.73,167.15A8,8,0,0,1,64,155.28l32-32,17.66,17.66a8,8,0,0,0,11.31,0L192,73.94v25.84a89.78,89.78,0,0,1-.85,11.74,8,8,0,0,0,6.85,9A8.15,8.15,0,0,0,199,120a8,8,0,0,0,7.93-7.13A112.55,112.55,0,0,0,208,99.78V88a8,8,0,0,0-13.66-5.66L130,146.74,112.32,129A8,8,0,0,0,101,129l-37.66,37.66A8,8,0,0,1,58.73,167.15Z"/>
                      </svg>
                    </div>
                    <div class="trust-badge__content">
                      <div class="trust-badge__title">6 meses de garantía</div>
                      <div class="trust-badge__description">por defectos</div>
                    </div>
                  </div>

                  <div class="trust-badge">
                    <div class="trust-badge__icon">
                      <svg viewBox="0 0 256 256" fill="currentColor">
                        <path d="M224,48H32A16,16,0,0,0,16,64V192a16,16,0,0,0,16,16H224a16,16,0,0,0,16-16V64A16,16,0,0,0,224,48Zm0,16V88H32V64Zm0,128H32V104H224v88Zm-16-24a8,8,0,0,1-8,8H168a8,8,0,0,1,0-16h32A8,8,0,0,1,208,168Zm-64,0a8,8,0,0,1-8,8H120a8,8,0,0,1,0-16h16A8,8,0,0,1,144,168Z"/>
                      </svg>
                    </div>
                    <div class="trust-badge__content">
                      <div class="trust-badge__title">Pago seguro,</div>
                      <div class="trust-badge__description">hasta 12 cuotas sin recargo</div>
                    </div>
                  </div>

                  <div class="trust-badge">
                    <div class="trust-badge__icon">
                      <svg viewBox="0 0 256 256" fill="currentColor">
                        <path d="M201.89,54.66A103.43,103.43,0,0,0,128.79,24H128A104,104,0,0,0,24,128v56a24,24,0,0,0,24,24H64a24,24,0,0,0,24-24V144a24,24,0,0,0-24-24H40.36A88.12,88.12,0,0,1,190.54,65.93,87.39,87.39,0,0,1,215.65,120H192a24,24,0,0,0-24,24v40a24,24,0,0,0,24,24h24a24,24,0,0,1-24,24H136a8,8,0,0,0,0,16h56a40,40,0,0,0,40-40V128A103.41,103.41,0,0,0,201.89,54.66ZM64,136a8,8,0,0,1,8,8v40a8,8,0,0,1-8,8H48a8,8,0,0,1-8-8V136Zm128,56a8,8,0,0,1-8-8V144a8,8,0,0,1,8-8h24v56Z"/>
                      </svg>
                    </div>
                    <div class="trust-badge__content">
                      <div class="trust-badge__title">Soporte 24/7</div>
                      <div class="trust-badge__description">por WhatsApp</div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>

        {%- else -%}
          <div
            class="cart-drawer__header"
            id="cart-drawer-header"
          >
            <span class="cart-drawer__heading h3" data-cart-count="{{ cart.item_count }}">
              {{ cart.item_count }} 
              {%- if cart.item_count == 1 -%}
                Artículo en el carrito
              {%- else -%}
                Artículos en el carrito
              {%- endif -%}
            </span>

            <button
              ref="closeButton"
              on:click="cart-drawer-component/close"
              class="button close-button cart-drawer__close-button button-unstyled"
              aria-label="{{ 'actions.close_dialog' | t }}"
            >
              <span class="svg-wrapper">
                {{- 'icon-close.svg' | inline_asset_content -}}
              </span>
            </button>
          </div>

          {%- comment -%} Barra de progreso de envío gratis {%- endcomment -%}
          {% assign free_shipping_threshold = 10000 %}
          {% assign cart_total = cart.total_price %}
          {% assign remaining = free_shipping_threshold | minus: cart_total %}
          {% assign progress_percentage = cart_total | times: 100.0 | divided_by: free_shipping_threshold %}
          {% if progress_percentage > 100 %}
            {% assign progress_percentage = 100 %}
          {% endif %}
          
          <div class="cart-drawer__free-shipping-bar">
            <div class="free-shipping-bar__text">
              {% if remaining > 0 %}
                Debes agregar al menos una cafetera de la familia Aurora para tener envío gratis
              {% else %}
                <strong>¡Felicitaciones, haz desbloqueado el envío gratis!</strong>
              {% endif %}
            </div>
            <div class="free-shipping-bar__progress">
              <div class="free-shipping-bar__progress-fill" style="width: {{ progress_percentage }}%"></div>
            </div>
          </div>

          <div
            class="cart-drawer__content motion-reduce"
            aria-label="{{ 'accessibility.cart' | t }}"
            style="--header-height: 60px;"
          >
            <scroll-hint
              class="cart-drawer__items"
            >
              {% render 'cart-products' %}
            </scroll-hint>

            {%- comment -%} Trust Badges - Justo después de los productos {%- endcomment -%}
            <div class="cart-drawer__trust-badges cart-drawer__trust-badges--after-products">
              <div class="trust-badges__grid">
                <div class="trust-badge">
                  <div class="trust-badge__icon">
                    <svg viewBox="0 0 256 256" fill="currentColor">
                      <path d="M208,40H48A16,16,0,0,0,32,56v58.77c0,89.61,75.82,119.34,91,124.39a15.53,15.53,0,0,0,10,0c15.2-5.05,91-34.78,91-124.39V56A16,16,0,0,0,208,40Zm0,74.79c0,78.42-66.35,104.62-80,109.18-13.53-4.51-80-30.69-80-109.18V56H208ZM58.73,167.15A8,8,0,0,1,64,155.28l32-32,17.66,17.66a8,8,0,0,0,11.31,0L192,73.94v25.84a89.78,89.78,0,0,1-.85,11.74,8,8,0,0,0,6.85,9A8.15,8.15,0,0,0,199,120a8,8,0,0,0,7.93-7.13A112.55,112.55,0,0,0,208,99.78V88a8,8,0,0,0-13.66-5.66L130,146.74,112.32,129A8,8,0,0,0,101,129l-37.66,37.66A8,8,0,0,1,58.73,167.15Z"/>
                    </svg>
                  </div>
                  <div class="trust-badge__content">
                    <div class="trust-badge__title">6 meses de garantía</div>
                    <div class="trust-badge__description">por defectos</div>
                  </div>
                </div>

                <div class="trust-badge">
                  <div class="trust-badge__icon">
                    <svg viewBox="0 0 256 256" fill="currentColor">
                      <path d="M224,48H32A16,16,0,0,0,16,64V192a16,16,0,0,0,16,16H224a16,16,0,0,0,16-16V64A16,16,0,0,0,224,48Zm0,16V88H32V64Zm0,128H32V104H224v88Zm-16-24a8,8,0,0,1-8,8H168a8,8,0,0,1,0-16h32A8,8,0,0,1,208,168Zm-64,0a8,8,0,0,1-8,8H120a8,8,0,0,1,0-16h16A8,8,0,0,1,144,168Z"/>
                    </svg>
                  </div>
                  <div class="trust-badge__content">
                    <div class="trust-badge__title">Pago seguro,</div>
                    <div class="trust-badge__description">hasta 12 cuotas sin recargo</div>
                  </div>
                </div>

                <div class="trust-badge">
                  <div class="trust-badge__icon">
                    <svg viewBox="0 0 256 256" fill="currentColor">
                      <path d="M201.89,54.66A103.43,103.43,0,0,0,128.79,24H128A104,104,0,0,0,24,128v56a24,24,0,0,0,24,24H64a24,24,0,0,0,24-24V144a24,24,0,0,0-24-24H40.36A88.12,88.12,0,0,1,190.54,65.93,87.39,87.39,0,0,1,215.65,120H192a24,24,0,0,0-24,24v40a24,24,0,0,0,24,24h24a24,24,0,0,1-24,24H136a8,8,0,0,0,0,16h56a40,40,0,0,0,40-40V128A103.41,103.41,0,0,0,201.89,54.66ZM64,136a8,8,0,0,1,8,8v40a8,8,0,0,1-8,8H48a8,8,0,0,1-8-8V136Zm128,56a8,8,0,0,1-8-8V144a8,8,0,0,1,8-8h24v56Z"/>
                    </svg>
                  </div>
                  <div class="trust-badge__content">
                    <div class="trust-badge__title">Soporte 24/7</div>
                    <div class="trust-badge__description">por WhatsApp</div>
                  </div>
                </div>
              </div>
            </div>

            <div
              class="cart-drawer__summary"
            >
              {%- comment -%} Aquí va el resumen del carrito (incluye trust badges antes del checkout) {%- endcomment -%}
              <div class="cart-summary-wrapper">
                {% render 'cart-summary' %}
              </div>
              
              {%- comment -%} Iconos de pago - Al final de todo {%- endcomment -%}
              <div class="cart-drawer__payment-icons">
                <img src="https://cdn.shopify.com/s/files/1/0726/2679/8780/files/visa-svgrepo-com.svg?v=1759308071" alt="Visa" class="payment-icon">
                <img src="https://cdn.shopify.com/s/files/1/0726/2679/8780/files/mastercard-full-svgrepo-com.svg?v=1759308071" alt="Mastercard" class="payment-icon">
                <img src="https://cdn.shopify.com/s/files/1/0726/2679/8780/files/paypal-svgrepo-com.svg?v=1759308071" alt="PayPal" class="payment-icon">
                <img src="https://cdn.shopify.com/s/files/1/0726/2679/8780/files/apple-pay-svgrepo-com.svg?v=1759307958" alt="Apple Pay" class="payment-icon">
                <img src="https://cdn.shopify.com/s/files/1/0726/2679/8780/files/google-pay-svgrepo-com.svg?v=1759308071" alt="Google Pay" class="payment-icon">
                <img src="https://cdn.shopify.com/s/files/1/0726/2679/8780/files/stripe-svgrepo-com.svg?v=1759307937" alt="Stripe" class="payment-icon">
              </div>
            </div>
          </div>
        {%- endif -%}
      </cart-items-component>
    </div>
  </dialog>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      const FREE_SHIPPING_THRESHOLD = 10000;
      
      function updateShippingBar(cartTotal) {
        const remaining = FREE_SHIPPING_THRESHOLD - cartTotal;
        const percentage = Math.min((cartTotal / FREE_SHIPPING_THRESHOLD) * 100, 100);
        
        const progressBars = document.querySelectorAll('.free-shipping-bar__progress-fill');
        progressBars.forEach(bar => {
          bar.style.width = percentage + '%';
        });
        
        const textElements = document.querySelectorAll('.free-shipping-bar__text');
        textElements.forEach(element => {
          if (remaining > 0) {
            element.innerHTML = 'Debes agregar al menos una cafetera de la familia Aurora para tener envío gratis';
          } else {
            element.innerHTML = '<strong>¡Felicitaciones, haz desbloqueado el envío gratis!</strong>';
          }
        });
      }
      
      function updateCartHeader(itemCount) {
        const headerElements = document.querySelectorAll('.cart-drawer__heading');
        headerElements.forEach(element => {
          const itemText = itemCount === 1 ? 'Artículo' : 'Artículos';
          element.textContent = `${itemCount} ${itemText} en el carrito`;
        });
      }
      
      function refreshCartDrawer() {
        fetch('/cart.js', {
          method: 'GET',
          headers: {
            'Content-Type': 'application/json',
          }
        })
        .then(response => response.json())
        .then(cart => {
          updateShippingBar(cart.total_price);
          updateCartHeader(cart.item_count);
          
          const cartBubbles = document.querySelectorAll('.cart-bubble');
          cartBubbles.forEach(bubble => {
            bubble.textContent = cart.item_count;
          });
        })
        .catch(error => {
          console.error('Error updating cart drawer:', error);
        });
      }
      
      document.addEventListener('cart:updated', refreshCartDrawer);
      document.addEventListener('cart:added', refreshCartDrawer);
      document.addEventListener('cart:change', refreshCartDrawer);
      
      const originalFetch = window.fetch;
      window.fetch = function(...args) {
        return originalFetch.apply(this, args).then(response => {
          const url = args[0];
          if (typeof url === 'string' && (url.includes('/cart/add') || url.includes('/cart/change'))) {
            setTimeout(refreshCartDrawer, 100);
          }
          return response;
        });
      };
      
      refreshCartDrawer();
    });
  </script>
</cart-drawer-component>

{% stylesheet %}
  /* ============================================
     ESTRUCTURA BASE
     ============================================ */
  .cart-items-component {
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
  }

  .cart-drawer__heading .cart-bubble {
    width: fit-content;
    border-radius: var(--style-border-radius-buttons-primary);
    aspect-ratio: auto;
    padding: var(--cart-padding);
  }

  .cart-drawer__heading .cart-bubble[data-maintain-ratio] {
    aspect-ratio: 1;
    min-width: 26px;
  }

  .cart-drawer__header {
    background-color: var(--color-background);
    display: flex;
    align-items: center;
    justify-content: flex-start;
    width: 100%;
    padding: var(--cart-drawer-padding, 20px);
    border-bottom: var(--style-border-width, 1px) solid var(--color-border, #e5e5e5);
    position: sticky;
    top: 0;
    z-index: 1;
  }
  
  .cart-drawer__header .cart-drawer__heading {
    flex: 1;
  }
  
  .cart-drawer__header .cart-drawer__close {
    margin-left: auto;
  }

  @media screen and (min-width: 750px) {
    .cart-drawer__header {
      padding: var(--cart-drawer-padding-desktop, 20px);
    }
  }

  .cart-drawer__dialog {
    overflow: hidden;
  }

  .cart-drawer__inner {
    height: 100%;
    overflow: hidden;
    display: flex;
    flex-direction: column;
  }

  .cart-drawer__content {
    height: calc(100% - var(--header-height, 60px));
    display: flex;
    flex-direction: column;
    flex: 1;
    overflow-y: auto;
  }

  .cart-drawer__summary {
    background-color: var(--color-background);
    position: sticky;
    bottom: 0;
    z-index: 1;
  }

  /* ============================================
     BARRA DE ENVÍO GRATIS
     ============================================ */
  .cart-drawer__free-shipping-bar {
    padding: 15px 20px;
    background: #f8f8f8;
    border-bottom: 1px solid var(--color-border, #e5e5e5);
  }

  .free-shipping-bar__text {
    font-size: 14px;
    color: var(--color-foreground, #333);
    margin-bottom: 8px;
    text-align: center;
  }

  .free-shipping-bar__text strong {
    font-weight: 600;
    color: #000;
  }

  .free-shipping-bar__progress {
    width: 100%;
    height: 6px;
    background: #e0e0e0;
    border-radius: 3px;
    overflow: hidden;
  }

  .free-shipping-bar__progress-fill {
    height: 100%;
    background: #4CAF50;
    transition: width 0.3s ease;
    border-radius: 3px;
  }

  /* ============================================
     ESTADO VACÍO
     ============================================ */
  .cart-drawer__empty-state {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 40px 20px;
    flex: 1;
    min-height: 200px;
  }

  .cart-drawer__empty-text {
    font-size: 18px;
    color: #666;
    margin-bottom: 20px;
    font-weight: 400;
  }

  .cart-drawer__cta-button {
    background: white;
    color: #333;
    border: 1px solid #d0d0d0;
    padding: 12px 30px;
    border-radius: 25px;
    font-size: 14px;
    cursor: pointer;
    transition: all 0.3s ease;
    text-decoration: none;
    display: inline-block;
    font-weight: 500;
  }

  .cart-drawer__cta-button:hover {
    background: #f5f5f5;
    transform: translateY(-2px);
    box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    text-decoration: none;
    color: #333;
  }

  /* ============================================
  /* ============================================
     TRUST BADGES - Dentro del cart summary, antes del checkout
     ============================================ */
  .cart-drawer__trust-badges {
    padding: 20px 16px 16px;
    margin-top: 16px;
    margin-bottom: 16px;
    background: transparent;
    width: 100%;
    overflow: visible;
    border-top: 1px solid var(--color-border, #e5e5e5);
  }

  /* Trust badges después de los productos (carrito lleno) */
  .cart-drawer__trust-badges--after-products {
    padding: 20px 20px;
    margin: 0;
    margin-bottom: 16px;
    border-top: none;
    border-bottom: 1px solid var(--color-border, #e5e5e5);
  }

  .trust-badges__grid {
    display: flex !important;
    flex-direction: row !important;
    flex-wrap: nowrap !important;
    justify-content: space-evenly;
    align-items: flex-start;
    gap: 16px;
    width: 100%;
  }

  .trust-badge {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 8px;
    flex: 0 1 auto;
    width: 30%;
    min-width: 80px;
    max-width: 110px;
  }

  .trust-badge__icon {
    width: 36px;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    margin: 0 auto;
  }

  .trust-badge__icon svg {
    width: 100%;
    height: 100%;
    fill: #795838;
  }

  .trust-badge__content {
    display: flex;
    flex-direction: column;
    gap: 2px;
    width: 100%;
  }

  .trust-badge__title {
    font-size: 11px;
    font-weight: 600;
    color: #1a1a1a;
    line-height: 1.2;
    white-space: nowrap;
  }

  .trust-badge__description {
    font-size: 9px;
    color: #666;
    line-height: 1.3;
    white-space: nowrap;
  }

  /* ============================================
     ICONOS DE PAGO - Más pequeños y en una línea
     ============================================ */
  .cart-drawer__payment-icons {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    padding: 10px 20px;
    background: #fff;
    flex-wrap: nowrap; /* Mantener en una sola línea */
    overflow-x: auto; /* Permitir scroll horizontal si es necesario */
  }

  .payment-icon {
    height: 40px; 
    min-width: auto;
    width: auto;
    max-width: 40px; /* Limitar ancho máximo */
    object-fit: contain;
    opacity: 0.7;
    flex-shrink: 0; /* Evitar que se encojan */
  }

  .payment-icon:hover {
    opacity: 1;
  }

  /* Ajuste para el summary */
  .cart-drawer__summary {
    background-color: var(--color-background);
    position: relative;
    z-index: 1;
    padding-bottom: 0;
  }

  /* ============================================
  /* ============================================
     RESPONSIVE
     ============================================ */
  @media (max-width: 480px) {
    .cart-drawer__free-shipping-bar {
      padding: 12px 15px;
    }

    .free-shipping-bar__text {
      font-size: 13px;
    }

    .cart-drawer__trust-badges {
      padding: 20px 12px;
      margin-top: 20px;
    }

    .trust-badges__grid {
      display: flex !important;
      flex-direction: row !important;
      flex-wrap: nowrap !important;
      gap: 12px;
      justify-content: space-evenly;
    }

    .trust-badge {
      flex: 0 1 auto;
      width: 30%;
      min-width: 70px;
      max-width: 95px;
      padding: 0;
    }

    .trust-badge__title {
      font-size: 9px;
      white-space: nowrap;
    }
    
    .trust-badge__description {
      font-size: 8px;
      white-space: nowrap;
    }
    
    .trust-badge__icon {
      width: 32px;
      height: 32px;
    }

    .cart-drawer__empty-text {
      font-size: 16px;
    }

    .cart-drawer__cta-button {
      padding: 10px 25px;
      font-size: 13px;
    }

    /* Iconos de pago aún más pequeños en móvil */
    .payment-icon {
      height: 40px;
      max-width: auto;
    }

    .cart-drawer__payment-icons {
      gap: 8px;
      padding: 8px 15px;
    }
  }

  /* ============================================
     ANIMACIONES
     ============================================ */
  @keyframes slideIn {
    from {
      transform: translateX(100%);
    }
    to {
      transform: translateX(0);
    }
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  .cart-drawer__dialog[open] {
    animation: slideIn 0.3s ease-out;
  }

  .cart-drawer__trust-badges {
    animation: fadeIn 0.5s ease-out;
  }

  /* ============================================
     UTILIDADES
     ============================================ */
  .cart-drawer--empty .cart-drawer__items {
    display: none;
  }

  .cart-drawer--empty .cart-drawer__summary {
    display: none;
  }

  /* ============================================
     SCROLLBAR PERSONALIZADO PARA PAYMENT ICONS
     ============================================ */
  .cart-drawer__payment-icons::-webkit-scrollbar {
    height: 0px;
  }

  .cart-drawer__payment-icons::-webkit-scrollbar-track {
    background: transparent;
  }

  .cart-drawer__payment-icons::-webkit-scrollbar-thumb {
    background: transparent;
  }
{% endstylesheet %}